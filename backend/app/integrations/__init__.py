"""Provider integration adapters for RecoveryOS.

Each sub-package adapts an external payment provider into the
provider-neutral RecoveryOS domain without coupling the core
decision engine to provider-specific code.
"""
